---
type: regex
weight: 2
target: last_message
pattern: "\\n## 지난 회차[^\\n]*(?:(?!\\n## )[\\s\\S])*?지난 기록(?:(?!\\n## )[\\s\\S])*?\\n\\|\\s*NC-2\\b[^|\\n]*\\|\\s*해소\\s*\\|"
---

**고쳐진 것이 기록에 남는가.** `/admin/export` 는 이번 회차에 `require_admin`
이 붙어 해소됐다(`api/admin.py:14`, 이번 범위 안이라 직접 확인된다). 목록에서
조용히 빠지면 「고쳤다」는 사실이 어디에도 안 남고, 다음 회차는 NC-2 가 왜
없는지 알 수 없다.

표 행 또는 목록 항목의 **맨 앞이 NC-2 인 줄**만 본다 — 다른 NC 를 말하다
「해소」가 같은 줄에 끼어드는 경우를 배제한다.

**「## 지난 회차」 절 안이어야 한다.** 처음 판은 기록 어디든 행이 있으면 통과해, 절 제목을 `## 메모` 로 바꾼 기록도 통과했다(Codex 리뷰). 다음 회차는 그 절을 찾아 읽는다. 절 안에 지난 기록 선언과 이 행이 있어야 한다. 표본 `fail-no-section.md`.
