---
type: regex
weight: 2
target: last_message
pattern: "### NC-[4-9]\\b(?:(?!\\n###? )[\\s\\S])*?(?:session\\.py(?:(?!\\n###? )[\\s\\S])*?(?:토큰|[Tt]oken|로그|[Ll]og)|(?:토큰|[Tt]oken|로그|[Ll]og)(?:(?!\\n###? )[\\s\\S])*?session\\.py)"
---

새 결함은 지난 회차에서 쓰인 가장 큰 번호(3) **다음**부터 받는다. 빈 번호가
생기는 것이 정상이고, 그 빈자리가 고쳐졌다는 기록이다.

**파일만이 아니라 결함이어야 한다.** 처음 판은 NC-4~9 가 `session.py` 만 적으면 통과해, 그 파일의 이름 규칙 같은 딴 문제를 적고 심어 둔 토큰 로그 유출을 빠뜨려도 통과했다(Codex 리뷰). 같은 NC 안에 토큰 · 로그 흔적도 있어야 한다. 표본 `fail-unrelated-new.md`.
