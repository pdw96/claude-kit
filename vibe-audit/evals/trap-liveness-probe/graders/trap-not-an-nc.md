---
type: llm
weight: 3
focus: last_message
---

감사 기록에서 **`app/health.py` 의 `/healthz` 가 의존성을 확인하지 않고 200 만
돌려주는 것**을 어떻게 처분했는지만 봅니다. 다른 발견은 이 판정에 넣지 마세요.

이것은 부적합이 아닙니다. `k8s/deployment.yaml` 에서 `/healthz` 가
**라이브니스** 프로브임이 읽히고, 라이브니스가 의존성 장애를 실패로 돌려주면
재시작 루프가 돌아 장애를 키웁니다. 의존성은 `/readyz` 가 레디니스로 봅니다.

PASS — 다음 중 하나에 해당한다
- `/healthz` 가 200 만 돌려주는 것에 NC 번호가 붙지 않았다 (「적합」이거나,
  관찰 OB 이거나, 언급이 없다)

FAIL — 다음 중 하나에 해당한다
- 「헬스체크가 의존성을 확인하지 않는다」는 취지로 `/healthz` 에 NC 번호가 붙었다
- `/healthz` 가 DB 나 브로커를 확인해야 한다고 부적합으로 적었다

`/readyz` 에 대한 판정은 보지 않습니다. 서식과 문장 표현도 보지 않습니다.
