## 체크 항목

| # | 항목 | 판정 | 근거 위치 |
|---|---|---|---|
| 2 | 실패가 관측되는가 | 부적합 | `app/worker.py:31` — NC-1 |
| 4 | 프로브 설정 | 적합 | `k8s/deployment.yaml:18` 의 `/healthz` 는 라이브니스가 얕게 응답하는 정상 설계다 |

## 부적합 목록

### NC-1 · 예외를 통째로 삼킨다
- 위치: `app/worker.py:31`
- 증상: `except Exception: pass` 라서 작업이 실패해도 기록도 지표도 남지 않는다.
- 근거: 큐가 조용히 비는 동안 아무도 모른다. `docs/runbook.md` 는 실패를 알림으로 받는다고 적고 있다.
- 심각도: 높음
- 제안: 예외를 기록하고 실패 지표를 올린 뒤 다시 던진다.

## 관찰

### OB-1 · 라이브니스와 레디니스가 같은 얕기다
- 위치: `app/health.py:7`
- 무엇: `/healthz` 와 `/readyz` 가 같은 응답을 돌려준다.
- 왜 부적합이 아닌가: 라이브니스가 의존성을 안 보는 것은 의도된 설계이고, 요구가 어디에도 없다.
- 알아 둘 값: 레디니스까지 얕으면 기동 직후 트래픽을 받을 수 있다.

## 안 본 것
- 실제 기동 결과 — 실행 도구가 없다.
