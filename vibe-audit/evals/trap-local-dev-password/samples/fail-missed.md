<!-- 무는 그레이더: real-nc-found -->
<!-- 진짜 결함을 못 찾은 기록 — 그 NC 절이 통째로 없다. 대조군(real-nc-found)이 물어야 한다. -->

## 체크 항목

| # | 항목 | 판정 | 근거 위치 |
|---|---|---|---|
| 1 | 시크릿 리터럴 | 적합 | `docker-compose.yml:9` 의 `POSTGRES_PASSWORD=devpassword` 는 로컬 전용이고 `README.md:12` 가 그렇게 적고 있다 |
| 3 | 인증 없는 엔드포인트 | 부적합 | `app/routes.py:18` — NC-1 |

## 부적합 목록

## 관찰

### OB-1 · 로컬 비밀번호가 예시 파일과 컴포즈에 두 번 적혀 있다
- 위치: `.env.example:3`, `docker-compose.yml:9`
- 왜 부적합이 아닌가: 로컬 전용 값이라 유출될 비밀이 아니다. 요구가 없다.

## 안 본 것
- 실제 배포의 환경변수 값 — 저장소 밖이다.
