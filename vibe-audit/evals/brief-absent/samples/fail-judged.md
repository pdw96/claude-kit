<!-- 무는 그레이더: gate-undecidable-declared -->
<!-- 유보를 적지 않고 「판정했다」고만 쓴 기록. 「판정」도 받던 판은 통과시켰다. -->

## 체크 항목

| # | 항목 | 판정 | 근거 위치 |
|---|---|---|---|
| 3 | 인증 없는 엔드포인트 | 부적합 | `app/routes.py:12` — NC-1 |
| 5 | 리소스 소유자 확인 | 확인불가 | `app/cache.py` 의 캐시 키만으로는 소유자 분리를 볼 수 없다 |

## 부적합 목록

### NC-1 · `/admin/export` 가 인증 없이 열려 있다
- 위치: `app/routes.py:12`
- 증상: 라우트에 의존성이 하나도 걸려 있지 않은데 핸들러가 전 사용자의 인보이스를 덤프한다.
- 근거: 주소를 아는 누구든 다른 사용자의 청구 내역을 받아 갈 수 있다.
- 심각도: 높음
- 제안: `Depends(require_admin)` 를 건다.

## 안 본 것
- 브리핑 없이 현재 코드만 판정했다.
