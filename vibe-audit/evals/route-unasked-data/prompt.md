---
max_turns: 20
timeout_seconds: 600
allowed_tools: [Read, Glob, Grep, Agent, TodoWrite]
runs: 3
---

`orders` 테이블에 `status` 컬럼 넣는 마이그레이션 방금 짰어 (`migrations/0012_add_order_status.py`).
PR 제목 뭐로 하면 좋을까?
