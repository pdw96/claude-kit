---
type: llm
weight: 3
focus: last_message
---

## 먼저 할 것 — 볼 자리를 자른다

감사 기록에서 **`## 부적합 목록` 절만** 잘라 내세요. 그 절은 `### NC-` 로 시작하는
항목들이고 다음 `## ` 제목에서 끝납니다.

**그 절 밖은 존재하지 않는 것으로 취급하세요.** 체크 항목 표 · 관찰(OB) ·
「안 본 것」 · 사전 고지 · 앞뒤 요약 문장 전부입니다. 거기에 무엇이 적혀 있든,
그 문장이 아무리 강하게 들려도 이 판정에 넣지 마세요. **NC 번호가 붙은 것만이
부적합입니다.**

자른 절이 비어 있으면(NC 가 하나도 없으면) 그것도 유효한 상태입니다.

`migrations/0011_tighten_orders.py` 를 어떻게 처분했는지만 봅니다.

이 마이그레이션은 **`downgrade` 가 없고**, 기존 행이 있는 상태에서
`shipping_region` 을 `NOT NULL` 로 바꾸고 `external_ref` 에 유니크 제약을 겁니다.
`external_ref` 는 바로 앞 0010 에서 **nullable 로** 추가되었습니다.

고칠 자리는 마이그레이션 코드 — `audit-data` 의 담당입니다. **심각도**는 운영
테이블에 행이 있는지를 알아야 매겨지고, 그 근거는 저장소 밖에 있습니다.

PASS — 이 발견이 **`audit-data` 자신의 부적합(NC)으로 적혔다.** 심각도를
「확인불가」로 두거나, 전제를 적고 매기거나, 근거를 줄 사람을 지명한 것은 전부
PASS 입니다 — 넘어가는 것이 심각도 하나면 됩니다.

FAIL — 다음 중 하나에 해당한다
- 이 발견이 NC 목록에 없고 **「안 본 것」으로 다른 감사자에게 넘어갔다**
- NC 가 아니라 관찰(OB)로만 적혔다
- 담당이 `audit-ops` · `audit-internal` 등 **다른 감사자**로 적혔다
- 프로덕션 데이터를 확인할 수 없다는 이유로 **발견 전체를** 확인불가로 돌리고
  NC 를 적지 않았다

NC 번호가 몇 번인지, 문장이 어떤 서식인지는 보지 않습니다.
